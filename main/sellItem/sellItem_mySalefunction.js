////////////////////myfunction////////////////////
function mySaleFunction(counter, price,itself) {
    tempcounter = parseFloat(counter) * parseFloat(price);
    tempcounter = tempcounter.toFixed(2);
	var p=itself.parentNode.parentNode;
	var ptds = p.getElementsByTagName("td")[12];
	ptds.children[0].value=tempcounter;
    
}
function mySaleFunction0(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price);
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target0").value = tempcounter;  
}

function mySaleFunction1(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price);
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target1").value = tempcounter;  
}

function mySaleFunction2(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target2").value = tempcounter;  
}

function mySaleFunction3(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target3").value = tempcounter;  
}

function mySaleFunction4(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target4").value = tempcounter;  
}

function mySaleFunction5(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target5").value = tempcounter;  
}

function mySaleFunction6(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target6").value = tempcounter;  
}

function mySaleFunction7(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target7").value = tempcounter;  
}

function mySaleFunction8(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target8").value = tempcounter;  
}

function mySaleFunction9(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target9").value = tempcounter;  
}

function mySaleFunction10(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target10").value = tempcounter;  
}

function mySaleFunction11(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target11").value = tempcounter;  
}

function mySaleFunction12(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target12").value = tempcounter;  
}

function mySaleFunction13(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target13").value = tempcounter;  
}

function mySaleFunction14(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target14").value = tempcounter;  
}

function mySaleFunction15(counter, price) {
    tempcounter = parseFloat(counter) * parseFloat(price); 
    tempcounter = tempcounter.toFixed(2);
    document.getElementById("target15").value = tempcounter;  
}